{
  "name": "heroy-school-management-system",
  "private": true,
  "version": "0.1.0",
  "description": "Heroy School Management System - AI-first enterprise school platform",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "npm run build --workspace=backend"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}